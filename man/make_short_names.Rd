% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_short_names.R
\name{make_short_names}
\alias{make_short_names}
\title{Shorten Column Names for Formats with a Name-Length Limit}
\usage{
make_short_names(x, max_length = 10)
}
\arguments{
\item{x}{Character vector of column names, without \code{NA}.}

\item{max_length}{Positive integer with the maximum name length in
bytes. Defaults to 10, the Shapefile limit.}
}
\value{
A character vector the same length as \code{x}.
}
\description{
Shortens a vector of column names so each fits within \code{max_length}
bytes and all are unique, ignoring case. Shapefiles store attribute
names in a \code{.dbf} file that allows at most 10 bytes per name, so
\code{\link[=export_shapefile]{export_shapefile()}} applies this function before writing a
Shapefile.
}
\details{
Names that already fit are kept unchanged. Longer names are
abbreviated with \code{\link[base:abbreviate]{base::abbreviate()}} and truncated to \code{max_length}
bytes. When two names collide, the later one gets a numeric suffix
such as \code{"_1"}. The result depends only on \code{x}, so calling the
function twice on the same names gives the same short names; this is
how \code{\link[=build_documentation]{build_documentation()}} records the names a Shapefile ships
with.
}
\examples{
make_short_names(c("id", "n_unidades_his_por_bloco", "n_unidades_hmp_por_bloco"))

}
\seealso{
\code{\link[=export_shapefile]{export_shapefile()}}, \code{\link[=build_documentation]{build_documentation()}}.
}
