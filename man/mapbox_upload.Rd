% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapbox_upload.R
\name{mapbox_upload}
\alias{mapbox_upload}
\title{Upload a Spatial Layer to Mapbox Studio (GeoPortal)}
\usage{
mapbox_upload(
  shp,
  username,
  tileset_id,
  tileset_name = NULL,
  access_token = Sys.getenv("MAPBOX_SECRET_TOKEN"),
  simplify = FALSE,
  keep_geojson = FALSE,
  multipart = TRUE,
  wait = FALSE,
  poll_interval = 5
)
}
\arguments{
\item{shp}{Either an \code{sf} object or a character path to an existing
GeoJSON / shapefile / MBTiles file that Mapbox's Uploads API
accepts.}

\item{username}{Mapbox username (the prefix of the full tileset ID).}

\item{tileset_id}{Character scalar with the short tileset slug (the
part \emph{after} the username, e.g. \code{"zoneamento_2024"}). Required.
Will be sanitised (lower-cased, special characters replaced with
\verb{_}) before submission.}

\item{tileset_name}{Optional human-readable name shown in Mapbox
Studio. Defaults to \code{tileset_id}.}

\item{access_token}{Mapbox secret token with \code{uploads:write} scope.
Defaults to the \code{MAPBOX_SECRET_TOKEN} environment variable.}

\item{simplify}{Geometry simplification. \code{FALSE} (default) writes
the layer as-is. \code{TRUE} calls \code{\link[rmapshaper:ms_simplify]{rmapshaper::ms_simplify()}} with
\code{keep = 0.05}. A numeric in \verb{(0, 1)} is passed as \code{keep} to
\code{ms_simplify()}.}

\item{keep_geojson}{Logical; when \code{shp} is an \code{sf} object, whether
to leave the intermediate GeoJSON file on disk after upload.}

\item{multipart}{Logical; pass through to
\code{\link[mapboxapi:upload_tiles]{mapboxapi::upload_tiles()}} for large files.}

\item{wait}{Logical. If \code{TRUE}, blocks until Mapbox reports the
upload as complete (or failed). If \code{FALSE} (default), returns
immediately after submission.}

\item{poll_interval}{Seconds between status polls when \code{wait = TRUE}.}
}
\value{
Invisibly returns the response from
\code{\link[mapboxapi:upload_tiles]{mapboxapi::upload_tiles()}} (or, if \code{wait = TRUE}, the final
status response from \code{\link[mapboxapi:check_upload_status]{mapboxapi::check_upload_status()}}).
}
\description{
Uploads an \code{sf} object (or an existing GeoJSON file) to Mapbox via
the Uploads API as a tileset, ready to use as a Mapbox Studio source.
Optionally simplifies the geometry first with
\code{\link[rmapshaper:ms_simplify]{rmapshaper::ms_simplify()}}.
}
\details{
Requires the suggested packages \code{mapboxapi} (always) and
\code{rmapshaper} (only when \code{simplify} is not \code{FALSE}).

The Uploads API caps individual uploads at roughly 300 MB
(uncompressed) or 260 MB GeoJSON. For larger layers, use
\code{\link[mapboxapi:mts_create_tileset]{mapboxapi::mts_create_tileset()}} / \code{\link[mapboxapi:mts_publish_tileset]{mapboxapi::mts_publish_tileset()}}
directly.
}
\examples{
\dontrun{
nc <- sf::st_read(system.file("shape/nc.shp", package = "sf"))

# Simplest path: write a tileset called "<your_user>.nc_counties"
mapbox_upload(
  nc,
  username   = "insper_cidados",
  tileset_id = "nc_counties"
)

# Aggressive simplification for a heavy polygon layer, then wait
# for Mapbox to finish processing before returning.
mapbox_upload(
  nc,
  username     = "insper_cidados",
  tileset_id   = "nc_counties_simplified",
  simplify     = 0.1,
  wait         = TRUE
)
}

}
\seealso{
\code{\link[=export_shapefile]{export_shapefile()}} for writing a layer to disk in
GeoJSON / GeoPackage formats before uploading.
}
