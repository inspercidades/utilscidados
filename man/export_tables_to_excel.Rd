% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_table_excel.R
\name{export_tables_to_excel}
\alias{export_tables_to_excel}
\title{Export a List of Data Frames to a Multi-sheet Excel Workbook}
\usage{
export_tables_to_excel(
  tables,
  file_name,
  font_name = "Arial",
  font_size = 11,
  header_font_size = 12
)
}
\arguments{
\item{tables}{A non-empty list of data frames. If the list has names,
they are used as sheet names; otherwise generic names (\code{"Sheet1"},
\code{"Sheet2"}, ...) are assigned. Sheet names longer than 31 characters
(Excel's limit) are truncated, and duplicates are made unique.}

\item{file_name}{Character scalar with the full path of the \code{.xlsx}
file to write. Required.}

\item{font_name}{Character scalar with the font family applied to the
workbook (default \code{"Arial"}).}

\item{font_size}{Numeric body font size (default \code{11}).}

\item{header_font_size}{Numeric header font size (default \code{12}).}
}
\value{
Invisibly returns \code{file_name}.
}
\description{
Writes a named list of data frames to a single \code{.xlsx} file, one sheet
per element, with a styled header row and auto-sized column widths.
}
\details{
Sheets where the corresponding list element is not a data frame are
skipped with a warning. The file is always written with
\code{overwrite = TRUE} at the \code{\link[openxlsx:saveWorkbook]{openxlsx::saveWorkbook()}} step; if the path
already exists a warning is emitted before writing.
}
\examples{
\dontrun{
tables <- list(
  iris = iris,
  mtcars = mtcars
)
export_tables_to_excel(tables, file_name = file.path(tempdir(), "out.xlsx"))
}

}
\seealso{
\code{\link[=export_table]{export_table()}} for single-table multi-format export.
}
